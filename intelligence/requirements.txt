# PocketLedger SA — Python Intelligence Service
# Install with: pip install -r requirements.txt

# API framework
fastapi==0.111.0
uvicorn[standard]==0.30.0

# Database (SQL Server)
pyodbc==5.1.0
SQLAlchemy==2.0.30

# AWS SDK
boto3==1.34.100

# Statement parsing
pdfplumber==0.11.0        # Extract text from PDF bank statements
openpyxl==3.1.2           # Parse Excel bank exports

# Data processing
pandas==2.2.2
python-dateutil==2.9.0

# OCR (Amazon Textract via boto3 — no extra lib needed)

# AI categorisation
openai==1.30.0            # Optional: swap with boto3 Bedrock if preferred

# Environment & config
python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.2.1

# Security
python-jose[cryptography]==3.3.0   # JWT token validation (from AWS Cognito)
passlib[bcrypt]==1.7.4

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0             # Test FastAPI endpoints
